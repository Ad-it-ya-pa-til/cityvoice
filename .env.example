# ============================================
# CITYVOICE ENVIRONMENT VARIABLES
# ============================================

# Port
PORT=3000

# ============================================
# FIREBASE ADMIN SDK
# ============================================
# Get your Firebase service account key from:
# Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# 
# Then encode it to base64:
# On Windows PowerShell: [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes((Get-Content serviceAccountKey.json -Raw)))
# On Linux/Mac: base64 -i serviceAccountKey.json
#
FIREBASE_SERVICE_ACCOUNT=your_base64_encoded_service_account_json_here

# ============================================
# JWT Authentication (Required for Admin)
# ============================================
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_64_characters_minimum
JWT_EXPIRE=7d

# ============================================
# CLOUDINARY (Optional - for image uploads)
# ============================================
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# ============================================
# NOTES:
# ============================================
# 1. Copy this file to .env (without .example)
# 2. Replace the placeholder values with your actual credentials
# 3. Never commit .env file to version control
# 4. Keep your credentials secure
